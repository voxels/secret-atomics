[build]
  command = "pnpm build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "24"

# Headers configuration
[[headers]]
  for = "/*"
  [headers.values]
    X-Powered-By = "Medal Social"
    
# Redirects are handled by next.config.ts
# Netlify will respect Next.js redirects automatically
